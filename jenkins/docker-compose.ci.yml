services:
  jenkins:
    build: ./
    container_name: jenkins
    ports:
      - "8080:8080"
      - "50000:50000"
    # user: "1083:1083"

    volumes:
      - jenkins_home:/var/jenkins_home
      - /run/user/1083/docker.sock:/var/run/docker.sock
volumes:
  jenkins_home: